---
# agent/tasks/user.yml

- name: Create sentinel service account
  tags: [deploy]
  ansible.builtin.user:
    name: "{{ sentinel_user }}"
    system: true
    shell: /usr/sbin/nologin
    home: "{{ sentinel_path }}"
    create_home: false
    state: present
