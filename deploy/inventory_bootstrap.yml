all:
  hosts:
    vps:
      ansible_host: myoracle.dynamic-dns.net
      ansible_user: ubuntu
      ansible_ssh_private_key_file: "{{ playbook_dir }}/ubuntu.key"
      ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
